# All inference runs locally via Ollama — no cloud APIs needed.
requests>=2.31.0

# Optional: Whisper transcription for audio/video content hints
# Uncomment to enable (requires ~1-5 GB for model weights + PyTorch):
# openai-whisper>=20231117
