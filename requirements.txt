# All inference runs locally — no cloud APIs.
requests>=2.31.0

# Whisper: transcribe + translate audio/video to English for intelligent naming.
# Required for video/audio files. Pulls PyTorch on first install (~700MB).
openai-whisper>=20231117
