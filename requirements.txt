requests>=2.31.0

# Gemini API (default provider) — vision for images/video, text for everything else.
google-genai>=1.0.0

# Whisper: optional, used only when provider=whisper-only. Pulls PyTorch (~700MB).
# openai-whisper>=20231117
